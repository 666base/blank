# Blank Hooks

> This directory will be moved to `@blank/worksapce/blank/hooks` in the future.

Only put hooks in this directory if they are specific to Blank, for example
if they are using the Blank API, or if the `BlankWorkspace` is required.
