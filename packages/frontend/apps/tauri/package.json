{
  "name": "@affine/tauri",
  "version": "0.27.0",
  "private": true,
  "description": "Blank desktop + Android shell (Tauri 2)",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build",
    "web:dev": "affine @affine/web dev",
    "web:build": "affine @affine/web build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@affine-tools/cli": "workspace:*",
    "@tauri-apps/cli": "^2.11.4"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-shell": "^2.3.5"
  }
}
